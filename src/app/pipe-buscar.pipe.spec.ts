import { PipeBuscarPipe } from './pipe-buscar.pipe';

describe('PipeBuscarPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeBuscarPipe();
    expect(pipe).toBeTruthy();
  });
});
